

#include <iostream>
#include <string>
using namespace std;

int main() {
    string str;
    int count = 0;

    cout << "Enter a string: ";
    cin >> str;

    while(str[count] != '\0') {
        count++;
    }

    cout << "Length of string: " << count;

    return 0;
}

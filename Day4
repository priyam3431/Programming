//swapping of two number easy method...


#include <stdio.h>

int main()
{
  int a=1,b=2;
  int c=a;
  printf("Before swapping a=%d and b=%d\n",a,b);
  a=b;
  b=c;
  printf("After swapping a=%d and b=%d",a,b);

    return 0;
}
